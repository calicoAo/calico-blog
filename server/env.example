# 数据库配置
MONGODB_URI=mongodb://192.168.0.103:27017/calicosBlog
# 服务器配置
PORT=3001  # 改为3001，避免与 macOS AirPlay 冲突（AirPlay 默认使用5000端口）
NODE_ENV=development

# JWT 配置
JWT_SECRET=your-super-secret-jwt-key-here
JWT_EXPIRES_IN=7d

# 其他配置
BCRYPT_ROUNDS=12

# 默认管理员账户（可选，仅在首次初始化时使用）
ADMIN_EMAIL=admin@calico-blog.com
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123456

# 加密密钥（用于前端密码加密，必须与前端保持一致）
CRYPTO_SECRET=calico-blog-secret-key-2025

# Refresh Token 配置（可选，如果不设置则使用 JWT_SECRET）
JWT_REFRESH_SECRET=your-refresh-token-secret-key-here
JWT_REFRESH_EXPIRES_IN=7d
